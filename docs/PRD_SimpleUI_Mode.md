# 简洁版/标准版双模式前端 PRD

## 版本信息
- 文档日期：2026-02-02
- 适用范围：前端 UI 方案（不涉及后端改造）

## 背景与目标
- 现状：标准版以表格为主，满足“任务表格化”要求，但移动端体验不友好。
- 目标：在保持标准版不变的前提下，新增“简洁版”展示模式，移动端优先；桌面端可切换用于调试。

## 核心原则
1. 功能一致：简洁版与标准版功能、字段、权限保持一致，仅改变呈现方式。
2. 移动优先：卡片/列表优先，表格仅在需要对账/对比场景使用。
3. 低侵入切换：仅开发测试账号可见切换入口，避免干扰正式用户。
4. 可扩展：未来可向更多角色开放或作为正式模式切换。

## 入口与切换
- 入口位置：顶部导航左侧显示两个按钮。
- 模式名称：标准版 / 简洁版（按钮文字直接显示）。
- 可见角色：仅 dev_test 可见。

## 全局切换框架（简洁版/标准版）
- 共存方式：同页面内条件渲染，功能/字段/权限一致，仅展示方式切换，路由与权限逻辑不变。
- 入口位置：顶部左侧两个按钮，文本直接显示“标准版 / 简洁版”。
- 可见角色：仅 dev_test 可见切换入口，其他角色不可见简洁版。
- 默认模式：标准版。
- 模式持久化：localStorage（前端纯实现）。
- 组件规范：统一全局组件（简洁版卡片/状态图例等），各页面复用。

## 交互与样式原则
- 卡片为主：列表使用信息卡片展示核心字段。
- 表格为辅：在详情或“需要对账/对比”场景提供“切换表格”。
- 状态用颜色：卡片边框/角标颜色表示状态，页面顶部提供颜色图例。
- 图片展示：缩略图最多 3 张，超过以“+N”入口展示。
- 折叠标识：全局统一使用 Chevron（向下/向上）。

## 页面方案（已定稿）

### 1) 排班管理（简洁版）
**视图切换：场站视图 / 人员视图**

**筛选区（简洁版）**
- 默认收起：单行“当前筛选摘要” + 右侧“筛选”按钮。
- 展开方式：点击“筛选”展开筛选项，选项变更即触发过滤；保存后自动收起。
- 筛选项：保留与标准版一致（仅收起以节省空间）。

**场站视图**
- 场站卡片：显示场站名称、当月已排/未排统计。
- 展开后显示岗位卡片：已排班=绿色，未排班=灰色。
- 点击岗位卡片：打开岗位月历（周一到周日排列）。
  - 单元格显示人员姓名，最多 3 个，超出显示“+N”。
  - 点击日期：展示当天详情（卡片列表：人员）。

**人员视图**
- 人员卡片：姓名 + 岗位/场站 + 本月班次数。
- 点击人员卡片：打开个人月历（周一到周日排列）。

**切换表格**
- 页面右上角提供“切换表格”按钮，用于对账/对比场景。

**待确认**
- 异常标识规则（缺岗/未排/冲突等）暂未确定。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 排班管理                               [切换表格] [筛选] │
│ 当前筛选摘要：场站=… | 人员=… | 月份=…                     │
└───────────────────────────────────────────────┘
│ 场站视图：场站卡 -> 展开岗位卡 -> 点岗位看月历 -> 点日期看详情 │
│ 人员视图：人员卡 -> 点开月历 -> 点日期看详情                 │
```

### 2) 故障上报/维修（简洁版）
**列表卡片字段**
- 场站 / 设备编号 / 设备名称 / 安装地点 / 上报人 / 紧急程度。

**状态表现**
- 不显示状态文字，仅用颜色角标表示；页面顶部提供状态颜色图例（统一全局组件）。

**详情弹窗**
- 顶部显示“流转记录”（时间线）。
- Tab 切换：上报信息 / 派单信息 / 维修信息 / 验收信息。

**切换表格**
- 页面右上角提供“切换表格”按钮。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 故障上报/维修                          [切换表格] [筛选] │
│ 图例：颜色=状态（无状态文字）                               │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 设备名称/编号/场站/安装地点/上报人/紧急程度         状态角标 │
│ [查看详情]                                                    │
└───────────────────────────────────────────────┘
```

### 3) 临时任务（简洁版）
包含 3 个子页面：填报 / 完成情况记录 / 临时工作任务汇总表。

#### 3.1 临时任务填报（/temporary-tasks/fill）
**卡片字段（折叠态）**
- 任务名称 / 执行人 / 单位积分 / 数量。

**展开后**
- 完成情况（完成/未完成）、备注、审核状态等。

**可编辑规则（对齐现状）**
- 仅“填报模式 + 可提交”时可编辑。
- 单位积分：是否可编辑取决于任务设置（例如 unitPointsEditable=1）。
- 数量、完成情况、备注：在可提交时可编辑。

**操作**
- 提交按钮保留。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 临时任务填报                          [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 任务名称                           状态角标   ▾           │
│ 执行人：xxx  单位积分：[10]  数量：[2]                         │
│（展开）完成情况/备注/更多字段…   [提交]                        │
└───────────────────────────────────────────────┘
```

#### 3.2 临时任务完成情况记录（/temporary-tasks/history）
**列表排序**
- 待审核置顶，其余按提交时间倒序。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态仅显示：任务名称 / 执行人 / 单位积分 / 数量。
- 展开后显示：完成情况、提交时间、备注、审核信息、扣分信息（仅驳回显示）。

**操作**
- “审核”按钮保留，点击后弹出审核弹窗（通过/驳回 + 扣分信息）。

**分页**
- 每页 5 条。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 临时任务完成情况记录                  [切换表格] [筛选] │
│ 待审核置顶 | 每页5条                                       │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 任务名称                           审核状态角标  ▾         │
│ 执行人：xxx  单位积分：10  数量：2                            │
│（展开）完成情况/提交时间/审核人/扣分…   [审核]                │
└───────────────────────────────────────────────┘
```

#### 3.3 临时工作任务汇总表（/temporary-tasks/library）
**筛选区**
- 折叠筛选条（默认收起：摘要 + 筛选按钮），展开后保留与标准版一致筛选项。

**操作区**
- 单行操作按钮：批量删除 / 新增任务 / 批量导入 / 下载模板。

**列表呈现**
- 卡片为主，表格为辅（右上角“切换表格”）。
- 卡片默认折叠（Chevron）：折叠态仅显示任务名称 / 单位积分 / 数量。
- 批量选择：卡片左侧提供勾选（用于批量删除）。
- 展开态显示其余字段（任务类别/给分方式/场站/内容/规则/可修改/强制审核等）。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 临时工作任务汇总表                     [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [批量删除] [新增任务] [批量导入] [下载模板]                │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [ ] ■ 任务名称                                  ▾            │
│     单位积分：10   数量：2                                   │
│（展开）更多字段…                         [分配/编辑/删除]     │
└───────────────────────────────────────────────┘
```

### 4) 岗位工作（简洁版）
包含 3 个子页面：固定任务填报 / 岗位工作任务汇总表 / 岗位工作完成情况记录。

#### 4.1 固定任务填报（/position-work/field）
**列表排序**
- 未提交置顶。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态显示：任务名称 / 单位积分 / 数量。
- 展开后显示：场站/岗位/任务类别/给分方式/任务来源/审核状态/完成情况/备注。

**操作**
- 提交按钮保留。

#### 4.2 岗位工作任务汇总表（/position-work/management）
- 规则对标“临时工作任务汇总表”，字段以岗位工作现状为准。
- 简洁版不保留“申请填报”入口。

#### 4.3 岗位工作完成情况记录（/position-work/records）
- 规则对标“临时任务完成情况记录”（待审核置顶、折叠卡、审核弹窗、每页5条）。

**框线草图（总览）**
```text
固定任务填报：未提交置顶 + 折叠卡（任务/积分/数量）+ 展开填完成情况/备注 + 提交
任务汇总表：卡片+勾选+单行操作区+派发/编辑/删除
完成情况记录：待审核置顶 + 折叠卡（任务/执行人/积分/数量）+ 展开详情 + 审核弹窗
```

### 5) 安全自检 / 安全他检（简洁版）
**填写明细交互（对齐现状，仅调整呈现）**
- 表单结构：同页两张大卡（填报/记录查询），点击大卡展开内容区；提交成功后自动折叠回摘要态。
- 操作按钮布局：同一行，重置/退出在左侧，提交在右侧。

**安全自检：检查项目行样式**
- 每条检查项目一行：左侧两行文字（检查项目 + 检查标准），右侧“是/否”二选一。
- “是/否”控件为“格子内有字”的按钮样式（非圆点 radio）。
- 默认值：进入展开后默认选“是”。
- 选择“否”时：显示异常备注与照片上传区（字段与校验对齐现状）。

**安全他检：检查项目行样式**
- 每条检查项目一行：左侧两行文字（检查项目 + 检查标准），右侧“合格/不合格”二选一。
- “合格/不合格”控件为“格子内有字”的按钮样式（非圆点 radio）。
- 默认值：进入展开后默认选“合格”。
- 选择“不合格”时：显示不合格原因与照片上传区（字段与校验对齐现状）。

**框线草图（安全检查项目行）**
```text
┌───────────────────────────────────────────────┐
│ 检查项目名称（父项/子项）                 [ 是 ][ 否 ] │
│ 标准：xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx          │
│（若选“否”）异常备注 + 照片上传…                           │
└───────────────────────────────────────────────┘

┌───────────────────────────────────────────────┐
│ 检查项目名称（父项/子项）           [ 合格 ][ 不合格 ] │
│ 标准：xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx          │
│（若选“不合格”）不合格原因 + 照片上传…                   │
└───────────────────────────────────────────────┘
```
目标：同一页面上下两张大卡片，减少“页面切换”，移动端优先。

#### 5.1 安全自检（两大卡布局）
**大卡1：自检填报**
- 摘要：今日日期；已完成哪些工作性质；未完成时提示“请尽快完成…”，无需完成则提示“今日无待完成项”。
- 点击展开：勾选工作性质 + 显示检查项目（对齐现状 SafetyCheckItemList）。
- 提交后：卡片自动折叠回摘要态。
- 按钮布局：重置/退出在左侧，提交在右侧，三者同一行。

**大卡2：自检记录**
- 摘要：日期范围默认“本周（周一-周日）”；合格x/不合格y；最后提交时间。
- 展开后：记录以“小卡片列表”嵌在大卡内。
- 常用筛选常显：日期范围（默认本周，可自定义）/ 工作性质。
- 不常用筛选隐藏：检查结果、排序等。
- 每页 5 条。
- 右下角退出按钮：点击折叠该大卡。

#### 5.2 安全他检（两大卡布局）
**大卡1：新增他检**
- 摘要文案：用于对员工进行安全检查：发现问题及时记录并提交。
- 展开后字段对齐现状：场站 / 被检查人 / 工作性质 / 检查项目（SafetyCheckItemList：合格/不合格）/ 备注。
- 提交后：卡片自动折叠回摘要态。
- 按钮布局：重置/退出在左侧，提交在右侧，三者同一行。

**大卡2：检查记录查询**
- 摘要文案：用于查看员工安全检查记录（包含自检与他检）。
- 记录列表对齐现状：检查性质（自检/他检）在同一查询结果内出现。
- 常用筛选常显：被检查人 / 日期范围（默认本周，可自定义）/ 工作性质。
- 不常用筛选隐藏：检查性质、场站、检查结果、排序等。
- 每页 5 条。
- 右下角退出按钮：点击折叠该大卡。

**框线草图**
```text
┌ 安全自检 ┐
[自检填报大卡 ▾]  摘要：今日/完成情况… -> 展开后：工作性质+检查项目  [重置][退出]      [提交]
[自检记录大卡 ▾]  摘要：本周范围/合格x不合格y/最后提交… -> 展开后：筛选+内嵌小卡列表+分页  [退出]

┌ 安全他检 ┐
[新增他检大卡 ▾]  摘要：功能介绍… -> 展开后：场站/被检查人/工作性质/检查项目  [重置][退出]      [提交]
[记录查询大卡 ▾]  摘要：包含自检+他检… -> 展开后：常用筛选常显+内嵌小卡列表+每页5条+分页  [退出]
```

### 6) 卫生自检 / 卫生他检（简洁版）
**填写明细交互（对齐现状，仅调整呈现）**
- 责任区分组：一行责任区标题；下方为点位列表。
- 点位行样式：左侧两行文字（点位/子项 + 要求/标准），右侧“合格/不合格”二选一。
- “合格/不合格”控件为“格子内有字”的按钮样式（非圆点 radio）。
- 默认值：进入展开后默认选“合格”。
- 选择“不合格”时：显示不合格说明与照片区（字段与校验对齐现状）。

**框线草图（卫生点位行）**
```text
┌───────────────────────────────────────────────┐
│ 责任区：xxxx（分组标题）                                │
├───────────────────────────────────────────────┤
│ 点位：xxxx                             [ 合格 ][ 不合格 ] │
│ 要求：xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx           │
│（若选“不合格”）点位照片…                               │
└───────────────────────────────────────────────┘
```
布局与交互完全对标“安全自检/他检”，仅表单字段与记录字段对齐卫生现状。

#### 6.1 卫生自检（两大卡布局）
**大卡1：自检填报**
- 摘要：今日日期；今日是否已完成（含完成时间）；未完成时提示尽快完成。
- 展开后对齐现状：责任区/点位检查（HygieneAreaChecklist），包含点位结果（合格/不合格）、点位照片、责任区照片、说明等；已完成时展开展示当日提交内容（只读）。
- 提交后自动折叠。
- 按钮布局：重置/退出在左侧，提交在右侧。

**大卡2：自检记录**
- 日期范围默认“本周（周一-周日）”，可自定义。
- 常用筛选：日期范围 / 责任区。
- 不常用筛选隐藏：结果、排序等。
- 内嵌小卡列表 + 每页 5 条 + 退出折叠按钮。

#### 6.2 卫生他检（两大卡布局）
**大卡1：新增他检**
- 摘要文案：用于对员工进行卫生检查：发现问题及时记录并提交。（可与安全他检保持同款文案风格）
- 展开后对齐现状：场站 / 责任区 / 被检查人 / 检查项目（SafetyCheckItemList：合格/不合格，点位/子项）/ 备注。
- 提交后自动折叠。
- 按钮布局：重置/退出在左侧，提交在右侧。

**大卡2：检查记录查询**
- 记录列表包含自检与他检（对标现状 inspectionKind）。
- 常用筛选：被检查人 / 日期范围（默认本周，可自定义）/ 责任区。
- 不常用筛选隐藏：检查性质、场站、结果、排序等。
- 每页 5 条 + 退出折叠按钮。

### 7) 保养管理（简洁版）
包含4个Tab：保养工作 / 保养计划 / 保养计划岗位分配 / 保养工作记录。

#### 7.1 保养工作（/maintenance-task/work）
**列表排序**
- 未提交置顶，按设备编号分组。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态显示：设备名称 / 设备编号 / 保养周期 / 状态角标（待完成=灰色、已完成=绿色、延期=红色）。
- 展开后显示：
  - 安装位置
  - 工作项列表（每项一行：名称/规范/积分/[完成][未完成]按钮）
  - 保养工具（输入框）
  - 保养时长（数字输入，单位：小时）
  - 耗材使用（卡片内展开填写：名称/型号/规格/数量）
  - 配件更换（卡片内展开填写：名称/型号/规格/数量/更换原因）
  - 备注

**工作项控件**
- 样式：[完成][未完成] 格子按钮（对标安全检查的"是/否"）。
- 默认值：空选（必须选择才能提交）。
- 校验：所有工作项必须选择完成/未完成，否则提交按钮禁用。

**操作**
- 提交按钮保留。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 保养工作                             [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 设备名称 / 设备编号              周保养  状态角标  ▾ │
│（展开）安装位置：xxx                                   │
│ 工作项：                                              │
│   工作名称1 | 规范xxx | 积分10      [完成][未完成]    │
│   工作名称2 | 规范xxx | 积分5       [完成][未完成]    │
│ 保养工具：[________]  保养时长：[__]小时              │
│ 耗材使用：[+添加]  配件更换：[+添加]                  │
│ 备注：[________]                          [提交]      │
└───────────────────────────────────────────────┘
```

#### 7.2 保养计划（/maintenance-task/plan）
**筛选区**
- 折叠筛选条（摘要 + 筛选按钮），筛选项：关键词/场站/保养周期。

**操作区**
- 单行按钮：批量删除 / 新增 / 批量导入 / 下载模板 / 批量导出。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：设备名称 / 设备编号 / 保养周期。
- 展开态：场站 / 安装位置 / 保养标准列表（名称/规范/积分）。
- 左侧勾选（批量删除）。

**操作**
- 编辑 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 保养计划                             [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [批量删除] [新增] [批量导入] [下载模板] [批量导出]    │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [ ] ■ 设备名称 / 设备编号              月保养    ▾    │
│（展开）场站：xxx  安装位置：xxx                       │
│ 保养标准：                                           │
│   工作名称1 | 规范xxx | 积分10                       │
│   工作名称2 | 规范xxx | 积分5                        │
│                                    [编辑] [删除]     │
└───────────────────────────────────────────────┘
```

#### 7.3 保养计划岗位分配（/maintenance-task/position）
**筛选区**
- 折叠筛选条，筛选项：场站/岗位/设备编号/设备名称/安装位置。

**操作区**
- 单行按钮：批量导出 / 下载模板 / 批量导入 / 分配保养计划。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：场站 / 岗位 / 设备名称。
- 展开态：设备编号 / 安装位置。

**操作**
- 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 保养计划岗位分配                     [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [批量导出] [下载模板] [批量导入] [分配保养计划]       │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 场站A / 岗位X / 设备名称                      ▾    │
│（展开）设备编号：xxx  安装位置：xxx                   │
│                                         [删除]       │
└───────────────────────────────────────────────┘
```

#### 7.4 保养工作记录（/maintenance-task/records）
**列表排序**
- 待验收置顶，其余按工作日期倒序。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：设备名称 / 执行人 / 工作日期 / 状态角标（待完成=灰色、已完成=蓝色、已验收=绿色、未完成=红色）。
- 展开态：设备编号 / 安装位置 / 保养周期 / 积分 / 提交时间 / 验收信息（仅已验收显示：验收人/验收结果/扣分）。

**操作**
- 查看详情。
- 验收按钮（管理员，状态=已完成时）→ 弹窗验收（通过/不通过 + 扣分信息）。

**分页**
- 每页5条。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 保养工作记录                         [切换表格] [筛选] │
│ 待验收置顶 | 每页5条                                  │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 设备名称 / 执行人 / 2026-02-12      状态角标   ▾   │
│（展开）设备编号：xxx  安装位置：xxx                   │
│ 保养周期：月保养  积分：15  提交时间：10:30          │
│ 验收信息：验收人xxx / 通过 / 扣分0                   │
│                              [查看详情] [验收]       │
└───────────────────────────────────────────────┘
```

### 8) 维修派发单（/maintenance-dispatch）
**功能定位**
- 站长查看待派发的故障上报，并将其派发给维修人员。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：设备名称 / 故障类型 / 紧急程度角标（一般=灰、较急=黄、紧急=橙、非常紧急=红）/ 上报时间。
- 展开态：上报人 / 故障描述 / 初步判断。

**操作**
- 派发按钮 → 弹窗（选择维修人员 + 维修要求）。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 维修派发单                           [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 设备名称 / 故障类型          紧急程度角标  上报时间  ▾ │
│（展开）上报人：xxx                                     │
│ 故障描述：xxxxxxxxxxxxxxxxxxxxxxxx                    │
│ 初步判断：xxxxxxxxxxxxxxxxxxxxxxxx                    │
│                                         [派发]        │
└───────────────────────────────────────────────┘
```

### 9) 维修工作（/repair-work）
**功能定位**
- 维修人员查看和处理自己的维修任务。

**筛选区**
- 状态筛选：全部 / 待处理 / 进行中 / 已完成（单选按钮组）。
- 折叠筛选条（其他筛选项）。

**列表排序**
- 待处理置顶，按紧急程度排序。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：设备名称 / 故障类型 / 紧急程度角标 / 状态角标（待处理=灰、进行中=蓝、已完成=绿）。
- 展开态：维修要求 / 故障描述 / 派发时间。

**操作**
- 待处理状态：开始维修。
- 进行中状态：完成维修 → 弹窗（维修描述/更换配件/维修成本）。
- 已完成状态：查看详情。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 维修工作                             [切换表格] [筛选] │
│ 状态：[全部] [待处理] [进行中] [已完成]               │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 设备名称 / 故障类型      紧急程度角标  状态角标   ▾ │
│（展开）维修要求：xxxxxxxxxxxxxxxxxxxxxxxx            │
│ 故障描述：xxxxxxxxxxxxxxxxxxxxxxxx                   │
│ 派发时间：2026-02-12 10:30                          │
│                              [开始维修/完成维修]     │
└───────────────────────────────────────────────┘
```

### 10) 设备维修记录（/repair-records）
**功能定位**
- 完整的维修记录管理，支持全流程操作。

**快捷筛选标签**（常显）
- 全部 / 我上报的 / 我负责维修 / 待派发 / 待验收。

**筛选区**
- 折叠筛选条（状态/设备名称等）。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：维修单号 / 设备名称 / 状态角标（草稿=灰、已提交=蓝、已派发=黄、维修中=橙、待验收=紫、已验收=绿）。
- 展开态：场站 / 故障日期 / 上报人 / 维修人员 / 创建时间。

**操作**
- 查看详情。
- 编辑（草稿状态且是上报人）。
- 派发（已提交状态且是站长）→ 弹窗。
- 验收（待验收状态且是站长）→ 弹窗。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 设备维修记录                         [切换表格] [筛选] │
│ [全部] [我上报的] [我负责维修] [待派发] [待验收]      │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ WX20260212001 / 设备名称              状态角标  ▾  │
│（展开）场站：xxx  故障日期：2026-02-12               │
│ 上报人：xxx  维修人员：xxx                           │
│                    [查看详情] [编辑/派发/验收]       │
└───────────────────────────────────────────────┘
```

### 11) 设备管理（/device-faults/equipment）
**功能定位**
- 设备信息的增删改查和批量导入导出。

**筛选区**
- 折叠筛选条，筛选项：场站/设备编号/设备名称/安装地点。

**操作区**
- 单行按钮：批量删除 / 新增设备 / 批量导入 / 下载模板 / 批量导出。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：设备名称 / 设备编号 / 场站。
- 展开态：安装地点 / 规格 / 型号 / 材质。
- 左侧勾选（批量删除）。

**操作**
- 编辑 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 设备管理                             [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [批量删除] [新增设备] [批量导入] [下载模板] [批量导出] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [ ] ■ 设备名称 / 设备编号 / 场站A              ▾    │
│（展开）安装地点：xxx                                 │
│ 规格：xxx  型号：xxx  材质：xxx                      │
│                                    [编辑] [删除]    │
└───────────────────────────────────────────────┘
```

### 12) 维修任务汇总表（/device-faults/task-library）
**功能定位**
- 维修任务模板库管理，用于标准化维修任务和积分计算。

**筛选区**
- 折叠筛选条，筛选项：任务名称/任务类别/给分方式/积分可修改/数量可修改。

**操作区**
- 单行按钮：批量删除 / 新增任务 / 批量导入 / 下载模板 / 批量导出。

**卡片折叠/展开**
- 默认折叠（Chevron）。
- 折叠态：任务名称 / 单位积分 / 数量。
- 展开态：任务类别 / 给分方式 / 积分规则 / 数量可修改 / 积分可修改。
- 左侧勾选（批量删除）。

**操作**
- 编辑 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 维修任务汇总表                       [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [批量删除] [新增任务] [批量导入] [下载模板] [批量导出] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [ ] ■ 任务名称                                  ▾   │
│     单位积分：10   数量：1                          │
│（展开）任务类别：Ⅰ类  给分方式：奖扣结合式          │
│ 积分规则：xxxxxxxxxxxxxxxxxxxxxxxx                  │
│ 数量可修改：是  积分可修改：否                      │
│                                    [编辑] [删除]    │
└───────────────────────────────────────────────┘
```

### 13) 安全隐患（/safety-rectification）
**功能定位**
- 安全隐患的发起、整改、复核全流程管理。

**两大卡布局**（对标安全自检/他检）

**大卡1：隐患表单**
- 摘要文案：用于发起安全隐患记录：发现问题及时记录并提交。
- 展开后字段：场站（必填）/ 位置（必填）/ 隐患类别（必填，支持新增）/ 隐患描述（必填）/ 隐患处照片。
- 提交后自动折叠。
- 按钮布局：重置/退出在左侧，提交在右侧。

**大卡2：隐患记录查询**
- 摘要文案：查看隐患整改进度和复核结果。
- 常用筛选常显：场站 / 状态（待整改/已整改/已复核）/ 日期范围。
- 不常用筛选隐藏：隐患类别。
- 内嵌小卡列表 + 每页5条 + 退出折叠按钮。

**记录小卡片**
- 折叠态：表单编号 / 场站 / 隐患类别 / 状态角标（待整改=橙、已整改=蓝、已复核=绿）。
- 展开态：位置 / 隐患描述 / 发起人 / 发起时间 / 整改信息（已整改时显示）/ 复核信息（已复核时显示）。

**操作**
- 查看详情。
- 整改（站长，状态=待整改时）→ 弹窗。
- 复核（安全员，状态=已整改时）→ 弹窗。
- 编辑/删除（安全员，状态=待整改时）。

**框线草图**
```text
┌ 安全隐患 ┐
[隐患表单大卡 ▾]  摘要：发起隐患记录… -> 展开后：场站/位置/类别/描述/照片  [重置][退出]  [提交]
[记录查询大卡 ▾]  摘要：查看整改进度… -> 展开后：筛选+内嵌小卡列表+分页  [退出]
```

### 14) 安全检查项目管理（/safety-check-management）
**功能定位**
- 管理安全检查的工作性质和检查项目配置。

**操作区**
- 单行按钮：下载模板 / 批量导入 / 新增工作性质 / 批量导出。

**工作性质卡片**（外层）
- 默认折叠（Chevron）。
- 折叠态：工作性质名称 / 积分 / 默认必选标识（角标）/ 状态标签（启用=绿、禁用=灰）。
- 展开后：检查项目子卡片列表。
- 操作：编辑 / 删除。

**检查项目子卡片**（内层）
- 显示：类型标签（父项/父项联动/子项）/ 检查项目名称 / 检查标准。
- 子项缩进显示，标注触发值（是/否）。
- 状态标签（启用/禁用）。
- 操作：编辑 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 安全检查项目管理                     [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [下载模板] [批量导入] [新增工作性质] [批量导出]       │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 工作性质名称 / 积分10 / 必选  启用              ▾  │
│（展开）检查项目：                                    │
│   ┌─────────────────────────────────────────┐      │
│   │ [父项] 检查项目1 | 标准xxx        启用   │      │
│   │   └ [子项] 触发值=否 | 子项内容   启用   │      │
│   └─────────────────────────────────────────┘      │
│                                    [编辑] [删除]    │
└───────────────────────────────────────────────┘
```

### 15) 卫生工作管理（/hygiene-work-arrangement）
**功能定位**
- 卫生区域划分和卫生任务分配管理。

**双Tab布局**

#### 15.1 卫生区域划分

**筛选区**
- 折叠筛选条，筛选项：场站/责任区。

**操作区**
- 单行按钮：下载模板 / 批量导入 / 新增责任区 / 批量删除 / 批量导出。

**责任区卡片**（外层）
- 默认折叠（Chevron）。
- 折叠态：场站 / 责任区名称 / 积分。
- 展开后：卫生点列表。
- 左侧勾选（批量删除）。
- 操作：编辑 / 删除。

**卫生点列表**（内层）
- 显示：卫生点名称 / 工作要求及标准。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 卫生区域划分                         [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [下载模板] [批量导入] [新增责任区] [批量删除] [批量导出] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [ ] ■ 场站A / 责任区名称 / 积分10              ▾    │
│（展开）卫生点：                                      │
│   点位1 | 工作要求xxx                               │
│   点位2 | 工作要求xxx                               │
│                                    [编辑] [删除]    │
└───────────────────────────────────────────────┘
```

#### 15.2 卫生任务分配

**筛选区**
- 折叠筛选条，筛选项：场站/岗位/责任区。

**操作区**
- 单行按钮：新增分配 → 弹窗表单（场站/岗位/责任区）。

**分配卡片**
- 显示：场站 / 岗位 / 责任区。
- 操作：删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 卫生任务分配                         [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [新增分配]                                          │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 场站A / 岗位X / 责任区Y                           │
│                                         [删除]      │
└───────────────────────────────────────────────┘
```

### 16) 消息通知（/notifications）
**功能定位**
- 展示和管理系统消息通知，按业务类型分组展示。

**业务分组**（6个大卡片）
- 安全隐患 / 安全他检 / 卫生他检 / 临时任务 / 设备故障 / 系统提醒。

**分组卡片**
- 折叠态：分组名称 / 未读数角标。
- 展开后：通知小卡片列表 + 分页（每页2条）。

**通知小卡片**
- 显示：标题 / 时间（MM-DD HH:mm）/ 未读红点。
- 点击跳转到相关页面并标记已读。

**操作**
- 全部清除未读（页面级）。
- 清除未读（分组级）。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 消息通知                           [全部清除未读] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 安全隐患                              (3)  ▾ │
│（展开）                          [清除未读]    │
│   ┌─────────────────────────────────────┐    │
│   │ ● 通知标题xxx              02-12 10:30│    │
│   └─────────────────────────────────────┘    │
│   分页：< 1/2 >                              │
└───────────────────────────────────────────────┘
```

### 17) 用户管理（/user-management）
**功能定位**
- 系统用户的增删改查、批量导入导出、权限配置。

**筛选区**
- 折叠筛选条，筛选项：姓名/公司/角色/部门/场站/状态。

**操作区**
- 单行按钮：新增用户 / 批量导入 / 下载模板 / 批量导出。

**用户卡片**
- 默认折叠（Chevron）。
- 折叠态：姓名 / 用户名 / 角色 / 状态标签（启用=绿、禁用=灰）。
- 展开态：部门 / 公司 / 手机号 / 邮箱 / 所属场站 / 创建时间。

**操作**
- 编辑 / 重置密码 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 用户管理                             [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [新增用户] [批量导入] [下载模板] [批量导出]          │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 张三 / zhangsan / 操作岗              启用   ▾ │
│（展开）部门：xxx  公司：xxx                       │
│ 手机：138xxxx  邮箱：xxx@xxx.com                 │
│ 所属场站：场站A, 场站B                           │
│ 创建时间：2026-01-01 10:00                      │
│                    [编辑] [重置密码] [删除]      │
└───────────────────────────────────────────────┘
```

### 18) 组织架构（/organization-management）
**功能定位**
- 管理场站、部门、公司、角色等组织架构信息。

**4个Tab布局**

#### 18.1 场站管理
**筛选区**
- 折叠筛选条，筛选项：场站名称。

**操作区**
- 单行按钮：新增场站 / 批量导入 / 下载模板 / 批量导出。

**场站卡片**
- 折叠态：场站名称 / 状态标签。
- 展开态：描述。
- 操作：编辑 / 删除。

#### 18.2 部门管理
**筛选区**
- 折叠筛选条，筛选项：部门名称。

**操作区**
- 单行按钮：新增部门 / 批量导入 / 下载模板 / 批量导出。

**部门卡片**
- 折叠态：部门名称 / 部门编码 / 状态标签。
- 展开态：描述。
- 操作：编辑 / 删除。

#### 18.3 公司管理
**筛选区**
- 折叠筛选条，筛选项：公司名称。

**操作区**
- 单行按钮：新增公司 / 批量导入 / 下载模板 / 批量导出。

**公司卡片**
- 折叠态：公司名称 / 公司编码 / 状态标签。
- 展开态：描述。
- 操作：编辑 / 删除。

#### 18.4 角色管理
**筛选区**
- 折叠筛选条，筛选项：角色名称。

**操作区**
- 单行按钮：新增角色 / 批量导入 / 下载模板 / 批量导出。

**角色卡片**
- 折叠态：角色名称 / 角色编码 / 基准角色。
- 展开态：描述 / 模块权限摘要。
- 操作：编辑 / 删除（系统角色不可删除）。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 组织架构  [场站] [部门] [公司] [角色]   [切换表格] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [新增xxx] [批量导入] [下载模板] [批量导出]        │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 名称 / 编码                        启用    ▾  │
│（展开）描述：xxxxxxxxxxxxxxxxxxxxxxxx           │
│                                [编辑] [删除]   │
└───────────────────────────────────────────────┘
```

### 19) 修改密码（/change-password）
**功能定位**
- 用户修改自己的登录密码。

**简单表单页面**（标准版/简洁版共用）

**表单卡片**
- 当前密码（必填）。
- 新密码（必填，至少6位）。
- 确认新密码（必填）。

**密码要求提示**
- 密码长度至少6位。
- 建议包含字母、数字和特殊字符。

**操作**
- 确认修改 / 重置。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 修改密码                                        │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ 当前密码：[********]                            │
│ 新密码：  [________]                            │
│ 确认密码：[________]                            │
│ 提示：密码长度至少6位…                          │
│                            [重置] [确认修改]    │
└───────────────────────────────────────────────┘
```

### 20) 帮助与反馈（/help-feedback）
**功能定位**
- 提供常见问题解答、用户反馈提交、联系方式展示。

**卡片分区布局**

**大卡1：常见问题**
- 折叠面板列表（FAQ）。
- 点击问题展开答案。

**大卡2：意见反馈**
- 表单：反馈类型（必填）/ 反馈内容（必填）/ 联系方式 / 截图。
- 操作：提交反馈。

**大卡3：联系我们**
- 显示：技术支持热线 / 邮箱 / 工作时间。

**大卡4：反馈记录**（仅管理员可见）
- 筛选：用户/类型/内容。
- 卡片列表：用户 / 类型标签 / 内容 / 时间。
- 操作：查看 / 批量导出。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 帮助与反馈                                      │
└───────────────────────────────────────────────┘
┌ 常见问题 ▾ ┐
│ Q: 如何提交工时？                          ▾   │
│ Q: 如何上报故障？                          ▾   │
└───────────────────────────────────────────────┘
┌ 意见反馈 ▾ ┐
│ 反馈类型：[功能建议 ▾]                         │
│ 反馈内容：[________________]                   │
│                              [提交反馈]        │
└───────────────────────────────────────────────┘
┌ 联系我们 ▾ ┐
│ 技术支持：400-XXX-XXXX                        │
│ 邮箱：support@example.com                     │
└───────────────────────────────────────────────┘
```

### 21) 首页（/home）
**功能定位**
- 系统首页，展示欢迎信息、快捷操作、最近通知。

**三区块布局**

**区块1：欢迎区**
- 显示：用户姓名 + 当前日期和星期。

**区块2：快捷操作区**
- 图标网格布局（2x3或3x2）。
- 根据角色显示默认快捷操作。
- 支持自定义管理。
- 操作：管理按钮 → 弹窗配置。

**区块3：最近通知区**
- 通知小卡片列表（最多5条）。
- 显示：标题 / 时间 / 未读红点。
- 操作：查看全部 → 跳转消息通知页。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ 你好，张三                                      │
│ 2026年2月12日 星期四                           │
└───────────────────────────────────────────────┘
┌ 快捷操作                              [管理] ┐
│ ┌────┐ ┌────┐ ┌────┐                        │
│ │排班表│ │安全 │ │卫生 │                        │
│ │    │ │自检 │ │自检 │                        │
│ └────┘ └────┘ └────┘                        │
│ ┌────┐ ┌────┐ ┌────┐                        │
│ │固定 │ │设备 │ │安全 │                        │
│ │任务 │ │故障 │ │隐患 │                        │
│ └────┘ └────┘ └────┘                        │
└───────────────────────────────────────────────┘
┌ 最近通知                          [查看全部] ┐
│ ● 通知标题1                      02-12 10:30 │
│ ● 通知标题2                      02-11 15:20 │
│   通知标题3                      02-10 09:00 │
└───────────────────────────────────────────────┘
```

### 22) 登录页（/login）
**功能定位**
- 用户登录认证。

**简单表单页面**（标准版/简洁版共用）

**居中表单卡片**
- 用户名（必填）。
- 密码（必填，支持显示/隐藏）。
- 记住密码（复选框）。

**操作**
- 登录按钮。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│                   系统登录                      │
│                                                │
│   用户名：[________________]                   │
│   密  码：[********] 👁                        │
│   [ ] 记住密码                                 │
│                                                │
│              [    登录    ]                    │
└───────────────────────────────────────────────┘
```

### 23) PLC数据（/plc-records）
**功能定位**
- PLC数据的全方位管理：实时监控、配置管理、历史记录、数据分类。

**4个Tab布局**

#### 23.1 实时监控

**顶部状态区**
- PLC服务状态卡片：按IP显示各设备在线/离线状态。
- 最后更新时间。

**筛选区**
- 折叠筛选条，筛选项：场站/分类。
- 自动刷新开关（5秒刷新）。

**操作区**
- 单行按钮：批量导出。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：监控点名称 / 当前值 / 连接状态角标（在线=绿、离线=红）。
- 展开态：场站 / 分类 / 地址 / 数据类型 / 单位 / 状态（正常/异常）。

#### 23.2 配置管理

**操作区**
- 单行按钮：新增配置。

**配置卡片**
- 折叠态：配置名称 / 状态标签。
- 展开态：配置详情。
- 操作：编辑 / 删除。

#### 23.3 历史记录

**统计卡片区**
- 总记录数 / 各分类统计数据。

**筛选区**
- 折叠筛选条，筛选项：日期范围/场站/分类/汇总粒度。

**操作区**
- 单行按钮：批量导出 / 下载模板 / 批量导入。

**数据卡片**
- 折叠态：监控点名称 / 数值 / 时间。
- 展开态：场站 / 分类 / 地址。

#### 23.4 数据分类管理

**操作区**
- 单行按钮：新增分类。

**分类卡片**
- 显示：分类名称 / 类型（计量型/变化型/事件型）。
- 操作：编辑 / 删除。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ PLC数据  [实时监控] [配置管理] [历史记录] [分类管理] │
└───────────────────────────────────────────────┘
┌ PLC服务状态 ┐
│ 192.168.1.1 在线  192.168.1.2 离线            │
│ 最后更新：10:30:05                            │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ ■ 监控点名称 / 当前值：123.45        在线    ▾ │
│（展开）场站：xxx  分类：xxx  地址：xxx         │
│ 数据类型：REAL  单位：m³  状态：正常          │
└───────────────────────────────────────────────┘
```

### 24) PLC数据报表（/plc-data-report）
**功能定位**
- 生成和查看PLC数据的统计报表。

**筛选区**
- 折叠筛选条，筛选项：日期范围/场站/分类。

**操作区**
- 单行按钮：批量导出。

**2个Tab布局**

#### 24.1 计量型报表

**统计卡片区**
- 总用量 / 平均日用量 / 最高日用量。

**时间粒度选择**
- 日 / 周 / 月 / 年（单选按钮组）。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：日期 / 监控点 / 用量（蓝色加粗）。
- 展开态：场站 / 地址 / 起始值 / 结束值 / 单位。

**用量排名Top10**
- 小卡片列表：排名 / 日期 / 监控点 / 用量（红色加粗）。

#### 24.2 变化型报表

**时间粒度选择**
- 日 / 周 / 月 / 年（单选按钮组）。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：日期 / 监控点 / 平均值（加粗）。
- 展开态：场站 / 地址 / 最小值 / 最大值 / 样本数 / 单位。

**框线草图**
```text
┌───────────────────────────────────────────────┐
│ PLC数据报表  [计量型] [变化型]   [切换表格] [筛选] │
└───────────────────────────────────────────────┘
┌ 统计 ┐
│ 总用量：1234.56  平均日用量：123.45  最高：456.78 │
└───────────────────────────────────────────────┘
│ 时间粒度：[日] [周] [月] [年]                    │
┌───────────────────────────────────────────────┐
│ ■ 2026-02-12 / 监控点A / 用量：123.45       ▾  │
│（展开）场站：xxx  地址：xxx                     │
│ 起始值：100.00  结束值：223.45  单位：m³       │
└───────────────────────────────────────────────┘
┌ 用量排名 Top10 ┐
│ 1. 2026-02-10 / 监控点B / 456.78              │
│ 2. 2026-02-08 / 监控点A / 345.67              │
└───────────────────────────────────────────────┘
```

### 25) PLC可视化报表（/plc-visual-report）
**功能定位**
- 可视化展示PLC数据趋势和统计。

**响应式切换规则**
- 桌面端（≥768px）：显示图表。
- 移动端（<768px）：自动切换为数据卡片列表。

**筛选区**
- 折叠筛选条，筛选项：日期范围/场站/分类/周期。

**2个Tab布局**

#### 25.1 计量型图表

**图表区**（桌面端）
- 用量趋势图（折线图）。
- 用量排名Top10（柱状图）。

**数据卡片**（移动端）
- 用量趋势卡片列表：时间 / 总用量 / 环比变化（↑↓）。
- 用量排名Top10卡片列表：排名 / 监控点 / 用量。

#### 25.2 变化型图表

**图表区**（桌面端）
- 极值组合图（柱状图+折线图）。
- 箱线图。

**数据卡片**（移动端）
- 极值统计卡片列表：时间 / 最小值 / 平均值 / 最大值。
- 数据分布卡片：监控点 / 最小值 / 中位数 / 最大值 / 样本数。

**框线草图**
```text
桌面端：
┌───────────────────────────────────────────────┐
│ PLC可视化报表  [计量型] [变化型]         [筛选] │
└───────────────────────────────────────────────┘
┌───────────────────────────────────────────────┐
│ [用量趋势图 - 折线图]                          │
│ [用量排名Top10 - 柱状图]                       │
└───────────────────────────────────────────────┘

移动端：
┌───────────────────────────────────────────────┐
│ PLC可视化报表  [计量型] [变化型]         [筛选] │
└───────────────────────────────────────────────┘
┌ 用量趋势 ┐
│ 2026-02-12 / 总用量：1234.56 / ↑12.3%        │
│ 2026-02-11 / 总用量：1098.76 / ↓5.2%         │
└───────────────────────────────────────────────┘
┌ 用量排名 Top10 ┐
│ 1. 监控点A / 456.78                           │
│ 2. 监控点B / 345.67                           │
└───────────────────────────────────────────────┘
```

---

## 报表/积分统计模块

### 可视化页面通用规则
- 桌面端（≥768px）：显示图表。
- 移动端（<768px）：自动切换为数据卡片列表。
- 通过 CSS 媒体查询或 JS 检测实现响应式切换。

### 26) 维保数据报表（/reports）
**功能定位**
- 展示设备保养和故障维修的统计数据与趋势分析。

**筛选区**
- 折叠筛选条，筛选项：日期范围/场站。

**操作区**
- 单行按钮：批量导出。

**2个Tab布局**

#### 26.1 设备保养统计

**统计卡片区**
- 记录总数 / 已完成 / 待完成 / 逾期记录 / 累计工时。

**图表区**（桌面端）/ **数据卡片**（移动端）
- 保养设备分布（柱状图 / 设备排名卡片）。
- 保养完成率（饼图 / 完成率卡片）。
- 全年趋势（折线图 / 月度数据卡片）。

#### 26.2 设备故障统计

**统计卡片区**
- 故障总数 / 待处理 / 处理中 / 已结案 / 涉及设备。

**图表区**（桌面端）/ **数据卡片**（移动端）
- 故障设备分布（柱状图 / 设备排名卡片）。
- 维修状态分布（饼图 / 状态占比卡片）。
- 全年趋势（折线图 / 月度数据卡片）。

### 27) 积分明细表（/points-summary/summary/detail）
**功能定位**
- 展示人员任务积分的详细明细数据。

**筛选区**
- 折叠筛选条，筛选项：维度/日期范围/姓名/任务名称/任务类别/数据来源。

**操作区**
- 单行按钮：批量导出 / 下载模板 / 人工导入（管理员）。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：姓名 / 日期 / 任务名称 / 总分。
- 展开态：得分大类 / 任务类别 / 完成次数 / 最大值 / 平均值 / 最小值 / 数据来源。

### 28) 积分统计表（/points-summary/summary/stats）
**功能定位**
- 按人员汇总各类别积分，支持下钻查看明细。

**筛选区**
- 折叠筛选条，筛选项：统计维度/日期范围/姓名。

**操作区**
- 单行按钮：批量导出。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：日期 / 姓名 / 总计。
- 展开态：安全 / 卫生 / 维修 / 保养 / 固定工作 / 临时任务 / 自行申请 / 扣分。
- 点击各类别可下钻查看明细（弹窗或展开）。

### 29) 任务积分分析（/points-summary/summary/visual）
**功能定位**
- 多维度可视化分析人员积分数据。

**筛选区**
- 折叠筛选条，筛选项：可视化维度/统计维度/日期范围/人员选择。

**图表区**（桌面端）/ **数据卡片**（移动端）
- 积分分布对比（柱状图 / 人员积分卡片列表）。
- 积分构成占比（饼图 / 类别占比卡片）。
- 积分排行榜Top10（柱状图 / 排名卡片列表）。
- 能力雷达图（雷达图 / 能力评分卡片）。
- 可改进分析（扣分明细卡片列表）。

### 30) 周期性积分分析（/points-summary/summary/cycle）
**功能定位**
- 按日/月/年维度展示人员积分的周期性变化趋势。

**筛选区**
- 折叠筛选条，筛选项：日期范围/人员选择。

**图表区**（桌面端）/ **数据卡片**（移动端）
- 日累计积分（堆叠图 / 日期积分卡片列表）。
- 月累计积分（堆叠图 / 月度积分卡片列表）。
- 年累计积分（堆叠图 / 年度积分卡片列表）。

### 31) 应用小时积分计算（/points-summary/applied-hourly/calc）
**功能定位**
- 计算和修正人员的应用小时积分。

**筛选区**
- 记录月份选择器。

**数据卡片**
- 显示：姓名 / 6个月总积分 / 6个月工时 / 实际应用小时积分 / 修正应用小时积分。
- 操作：修改（行内编辑，0-10分，步长0.1）/ 保存 / 取消。

### 32) 应用小时积分历史记录（/points-summary/applied-hourly/history）
**功能定位**
- 展示人员历史各月的应用小时积分数据。

**筛选区**
- 折叠筛选条，筛选项：姓名。

**2个Tab布局**
- 实际应用小时积分表。
- 修正应用小时积分表。

**数据卡片**
- 显示：姓名 + 各月份积分（横向滚动或折叠展开）。

### 33) 工时导入（/points-summary/applied-hourly/import）
**功能定位**
- 管理人员工时数据的导入和维护。

**筛选区**
- 折叠筛选条，筛选项：月份范围/姓名。

**操作区**
- 单行按钮：下载模板 / 导入工时（管理员）。

**数据卡片**
- 显示：姓名 / 月份 / 工时(小时) / 备注 / 创建人 / 创建时间。
- 操作：编辑 / 删除。

### 34) 应用小时积分可视化分析（/points-summary/applied-hourly/visual）
**功能定位**
- 可视化展示人员应用小时积分排名。

**筛选区**
- 折叠筛选条，筛选项：统计截止月/人员选择。

**图表区**（桌面端）/ **数据卡片**（移动端）
- TOP10应用小时积分（柱状图 / 排名卡片列表）。
- 全量排名（柱状图 / 排名卡片列表，分页）。

### 35) 季度积分奖计算（/points-summary/quarterly-award/calc）
**功能定位**
- 管理季度积分奖分组并计算奖金分配。

**筛选区**
- 年份 / 季度选择器。

**操作区**
- 单行按钮：新建分组。

**分组卡片**（外层）
- 默认折叠（Chevron）。
- 折叠态：分组名称 / 人均预算 / 总预算。
- 展开后：
  - 分组配置：关联往期分组 / 人员选择 / 绩效分值 / 季度积分奖系数 / 积分总和。
  - 分组明细卡片列表（内层）：排名 / 姓名 / 季度积分 / 积分占比 / 个人季度积分奖。
- 操作：编辑 / 计算 / 删除。

### 36) 季度积分奖历史数据（/points-summary/quarterly-award/history）
**功能定位**
- 查询历史季度积分奖发放记录。

**筛选区**
- 折叠筛选条，筛选项：年份/季度/分组名称/姓名。

**数据卡片**
- 默认折叠（Chevron）。
- 折叠态：姓名 / 年份季度 / 个人季度积分奖。
- 展开态：分组 / 排名 / 季度积分 / 积分占比 / 季度积分奖系数。

### 37) 季度积分奖可视化分析（/points-summary/quarterly-award/visual）
**功能定位**
- 可视化展示季度积分奖的分布和趋势。

**筛选区**
- 折叠筛选条，筛选项：年份/季度/分组。

**图表区**（桌面端）/ **数据卡片**（移动端）
- 季度积分奖排名TOP10（柱状图 / 排名卡片列表）。
- 积分占比分布（饼图 / 占比卡片列表）。
- 季度积分奖趋势（折线图 / 趋势数据卡片列表）。

## 简洁版统一组件库规范

### 状态颜色定义（语义色）

| 颜色 | 色值 | 语义 | 适用场景 |
|------|------|------|---------|
| 绿色 | #52c41a | 完成/通过/启用/在线 | 已完成、已验收、已复核、启用、在线 |
| 蓝色 | #1890ff | 进行中/已提交 | 进行中、已提交、已完成待验收 |
| 橙色 | #fa8c16 | 待处理/警告 | 待整改、待验收、待派发、延期 |
| 红色 | #f5222d | 异常/逾期/紧急 | 未完成、逾期、离线、非常紧急 |
| 灰色 | #8c8c8c | 草稿/禁用/默认 | 草稿、禁用、待完成 |
| 黄色 | #faad14 | 较急/已派发 | 较急、已派发 |
| 紫色 | #722ed1 | 待验收 | 待验收（维修记录） |

### 卡片组件规范

| 属性 | 值 |
|------|-----|
| 圆角 | 8px |
| 阴影 | 0 2px 8px rgba(0,0,0,0.1) |
| 内边距 | 12px 16px |
| 外边距 | 8px 0 |
| 背景色 | #ffffff |

### 状态角标规范

| 属性 | 值 |
|------|-----|
| 样式 | 圆点 |
| 大小 | 8px |
| 位置 | 卡片右上角，距边缘12px |

### 图例组件规范

| 属性 | 值 |
|------|-----|
| 位置 | 筛选区下方，列表上方 |
| 布局 | 横向排列，自动换行 |
| 样式 | 圆点 + 文字说明 |

### 折叠标识规范

| 属性 | 值 |
|------|-----|
| 图标 | Chevron（向下/向上） |
| 位置 | 卡片右侧 |
| 动画 | 180度旋转，200ms |

### 内嵌小卡规范

| 属性 | 值 |
|------|-----|
| 左侧缩进 | 16px |
| 背景色 | #fafafa（比外层卡片略浅） |
| 圆角 | 4px |
| 内边距 | 8px 12px |

### 响应式断点

| 断点 | 宽度 | 说明 |
|------|------|------|
| 移动端 | <768px | 卡片列表，图表切换为数据卡片 |
| 桌面端 | ≥768px | 可切换表格，显示图表 |

## 待确认 / 待补充
- 无